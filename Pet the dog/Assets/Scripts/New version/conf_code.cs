using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class conf_code : MonoBehaviour
{
    // Start is called before the first frame update
    public bool can_you_pet = true;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
